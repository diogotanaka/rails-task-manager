<h2><%= @task.title %></h2>
<p><%= @task.details %>     <%= link_to "Delete", task_path(@task), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %>
</p>
<p><%= @task.completed %></p>
<%= link_to "Back to tasks", tasks_path, class:'btn btn-primary btn-sm' %>
<%= link_to "Edit Task", edit_path, class:'btn btn-primary btn-sm' %>
